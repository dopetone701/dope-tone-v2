// cc-email-composer.js - DEPRECATED - vault-emails.js is now the composer
// Kept to avoid V2 import errors - logic 100% moved to vault-emails.js
export function initEmailComposer() {
  // no-op - vault-emails.js auto-injects button
  return;
}
